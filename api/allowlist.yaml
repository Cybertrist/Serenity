# Sites the agent may rotate on its own. Checked by code only, never by an LLM.
#
# Strict format, one domain per line under "domains:". A domain also covers its subdomains
# (netflix.com covers www.netflix.com, not netflix.com.evil.example).
domains:
  # The demo site (compose profile "demo"): the agent's practice ground, not a real account.
  - demo.serenity.test
