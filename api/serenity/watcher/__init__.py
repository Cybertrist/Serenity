"""Breach watch (docs/05-veille.md): Pwned Passwords in k-anonymity, reused, weak, old passwords,
and watched e-mail addresses (HIBP). The same rules run in the browser (all zones) and in the
agent process (agent zone only).
"""
